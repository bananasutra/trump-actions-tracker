name: Keep App Awake
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -s https://trump-actions-tracker.streamlit.app/ > /dev/null
